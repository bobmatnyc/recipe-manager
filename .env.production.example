# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# This file shows the configuration needed for production deployment
# Copy relevant values to your production environment (Vercel, etc.)

# Application Configuration
NEXT_PUBLIC_APP_URL=https://recipes.help
NODE_ENV=production

# Database Configuration (Production)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# AI Features (Production)
OPENROUTER_API_KEY=sk-or-v1-your-production-api-key-here

# Authentication (Clerk) - PRODUCTION KEYS
# These are live keys restricted to recipes.help domain
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY_PROD=pk_live_Y2xlcmsucmVjaXBlcy5oZWxwJA
CLERK_SECRET_KEY_PROD=sk_live_kVahgqoC5y0pGHgfTjyxJoffbzYCh4OGI1AfwpXImg

# Alternative: Use the auto-selection method with live keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsucmVjaXBlcy5oZWxwJA
CLERK_SECRET_KEY=sk_live_kVahgqoC5y0pGHgfTjyxJoffbzYCh4OGI1AfwpXImg

# Clerk redirect URLs for production
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Security Settings (automatically enabled in production)
# These are set automatically by the auth configuration
# ENABLE_DEV_AUTH - not used in production
# ALLOW_LIVE_KEYS_IN_DEV - not applicable in production

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
#
# For Vercel deployment:
# 1. Set these environment variables in your Vercel dashboard
# 2. The system will automatically detect production environment
# 3. Live keys will be used automatically for recipes.help domain
#
# For other platforms:
# 1. Ensure NODE_ENV=production
# 2. Set NEXT_PUBLIC_APP_URL=https://recipes.help
# 3. Configure the Clerk keys above
# 4. Set your database and API keys
#
# Domain verification:
# - Production keys are restricted to recipes.help domain
# - Requests from localhost will be rejected in production
# - Make sure your domain is properly configured in Clerk Dashboard
#