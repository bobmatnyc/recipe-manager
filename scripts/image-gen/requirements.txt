# Image Generation Requirements
# For Apple Silicon M4 Max with Metal Performance Shaders (MPS)

# Core dependencies
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.25.0
transformers>=4.35.0
accelerate>=0.25.0

# Image processing
pillow>=10.0.0

# Model management
huggingface-hub>=0.19.0
safetensors>=0.4.0

# Optional: Better progress bars
tqdm>=4.66.0

# Optional: Memory optimization (skip on macOS - build issues)
# xformers>=0.0.23  # Only for Linux/Windows with CUDA
